# NOTE: Install with --prefer-binary to avoid Rust compilation on Python 3.14+
#   pip install -r requirements.txt --prefer-binary

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic>=2.7.0           # pydantic v2 — pre-built wheels required (no MSVC needed)
pydantic-settings>=2.3.0  # loads settings from env vars / .env file
python-dotenv==1.0.1
httpx==0.28.0
aiosqlite>=0.20.0         # async wrapper around sqlite3 — pure Python, no compilation
pytest==8.3.4
pytest-asyncio==0.24.0
asyncpg>=0.30.0
python-dateutil>=2.9.0
openai>=1.0.0
